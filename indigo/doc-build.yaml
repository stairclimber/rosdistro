%YAML 1.1
---
doc_tag_index_repository:
  type: master
  url: git@github.com/ros-infrastructure/rosdoc_tag_index
jenkins_job_timeout: 60
jenkins_url: http://buildfarmdeployment-master.dev.docker:8080
targets:
  _config:
    apt_mirrors:
    - http://buildfarmdeployment-repo.dev.docker/debian/building
    debian:
      wheezy:
        armhf: 
type: doc-build
version: 1
